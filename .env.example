# Resend API key (required for contact form emails)
RESEND_API_KEY=

# Email lead delivery – comma-separated list of admin recipients
LEADS_TO=we@magnasskexim.in

# Sender address used for all outbound emails
FROM_EMAIL=MSE Magna SSK Exim <we@magnasskexim.in>

# WhatsApp redirect link used across the site
NEXT_PUBLIC_WHATSAPP_URL=https://wa.link/e854rz

# Set to "true" to log leads to console when RESEND_API_KEY is missing (optional)
EMAIL_DEBUG=true
